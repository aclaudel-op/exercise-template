{
  "name": "first-draft",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:report": "jest --coverage --coverageReporters=html --coverageReporters=text",
    "test:acceptance": "jest --coverage --coverageReporters=html --coverageReporters=text --coverageDirectory=coverage-acceptance",
    "bun": "bun test",
    "echo-secret": "echo token=$MY_SECRET_TOKEN"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
